# Implementation Plans

Plans 001–004 generated by the improve skill on 2026-07-12; plans 005–007 added
on 2026-07-20 (an `improve next` direction run). Execute in the order below
unless dependencies say otherwise. Each executor must read its plan fully, honor
its STOP conditions, and update its row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 005 | Verification baseline: format-matrix tests + GitHub Actions CI | P1 | M | — | TODO |
| 001 | Save converted results as one ZIP | P1 | M | — | TODO |
| 002 | Add image resize, rotation, and metadata controls | P1 | M | — | TODO |
| 003 | Add downloader resolution and bitrate controls | P1 | M | — | TODO |
| 006 | Stream real conversion progress for video/audio (SSE) | P1 | L | 005 (rec.) | TODO |
| 004 | Add paste highlighting and owner-scoped search | P2 | M | — | DONE |
| 007 | Add one client-side developer utility (Encode & Hash spike) | P3 | S | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale)

## Recommended order (rationale)

1. **005** first — it is the verification gate; landing it means every plan below
   executes against a green CI (`biome check` + `tsc` + `vitest` + `build`) and
   supports the open-source-promotion goal in `VISION.md`.
2. **001 → 002** next — both change the `Job` model, `Workbench`, and
   `useWorkbench`; do 001 before 002 to minimize conflicts (unchanged from the
   original run).
3. **003** — independent downloader work.
4. **006** — the highest daily-use payoff, but the largest change (reshapes the
   convert request/response); best done after 005 exists and after 001 has
   settled the `Job`/`useWorkbench` shape it also touches.
5. **007** — spike-scoped; do any time, independent of the rest. (004 is DONE.)

## Dependency notes

- 001, 002, 003 are functionally independent of each other; 004 is DONE.
- 006 recommends 005 (a safety net before reshaping the convert path) and shares
  files with 001 (`use-workbench.ts`, the `Job` shape) — rebase 006 after 001 if
  both are in flight.
- 001 added a dependency; commit its `package.json`/`bun.lock` changes with the
  feature. 005, 006, and 007 add **no** runtime dependencies.
- 007 is a spike: ship one tool, then stop — do not expand it in the same PR.

## Direction findings from the 2026-07-20 `next` run

Surfaced, and the maintainer's disposition:

- **D1 conversion progress (SSE)** → plan **006** (selected).
- **D2 tests + CI** → plan **005** (selected).
- **D5 client-side dev utility** → plan **007** (selected as a spike).
- **D3 SEO: sitemap + pair landing routes** — grounded (recent llms.txt / og /
  social-meta commits show public discovery becoming a goal; `robots.txt` has no
  `Sitemap:` line and no `/images/jpg-to-png` pages exist), but **held** for now.
  Revisit when public discovery is an explicit priority.
- **D4 SSRF DNS pinning** for screenshot + downloader — real gap: `blockedUrl`
  (`src/server/limits.ts:55`) regex-matches the literal hostname and never
  resolves DNS, so a public host with an A record pointing at a private/link-local
  IP bypasses it. **Held**, not rejected — worth a dedicated security plan when
  hardening is prioritized (also named in `ROADMAP.md` "Stronger SSRF guarding").

## Findings considered and rejected (do not re-audit)

From the 2026-07-12 run:

- Persistent job queue: defer until failed/reloaded long-running jobs are a
  demonstrated recurring problem; adds storage, cleanup, concurrency, deployment
  complexity. (Note: plan 006's *ephemeral* progress registry is not this — it is
  the seam where a persistent queue would later plug in.)
- Video/audio editing: defer until higher-frequency conversion and downloader
  workflows are complete.
- i18n (English + Ukrainian): defer while product copy and tool surfaces are
  still changing quickly.
- Pair landing routes: previously deferred; re-surfaced as D3 above because SEO
  work has since started — still held, not built.
- Archive workspace: coherent direction, but needs usage validation before a
  dedicated multi-file UI and safe-extraction surface are built.
